var student = {
  firstName: 'Fabian',
  age: 41,
}

var ageInMonths = student.age * 12

function printAgeInMonths() {
  student.age * 12
}
// "I am " + student.firstName + " and I am " + student.age + " years old, or " +
// + " months."
printAgeInMonths()
