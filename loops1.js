// X Use a while loop to build it

//X Create a program that starts counting at 5.
//X It counts up to (including) 50,

//X and count only every fifth number.

var count = 0

while (count < 50) {
  count += 5
  console.log(count);
}
